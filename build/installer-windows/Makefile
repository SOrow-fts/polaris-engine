all:
	makensis -INPUTCHARSET UTF8 install-script.nsi

zip:
	rm -f polaris-engine.zip
	zip -r polaris-engine.zip polaris-engine.exe games tools

clean:
	rm -rf polaris-engine.exe games tools polaris-engine-installer.exe
